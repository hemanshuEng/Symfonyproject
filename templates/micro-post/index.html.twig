{% extends "base.html.twig" %}
{% block body %}
    <div class="my-3 p-3 bg-white rounded shadow">
        <small class="d-block text-right mt-3">
            <a href="{{path('micro_post_add')}}">Add New</a>
        </small>
        {% for post in posts %}
            <div class="media text-muted pt-3">
                <img alt="" class="m2-2 rounded" data-src="holder.js/32x32?text=MJ&bg=e83e8c&fg&size=8">
                <p class="media-body pb-3 mb-0 small lh-125 border-bottom border-grey">
                    <span class="d-block">
                        <strong class="text-grey-dark">@username</strong>
                        <small>{{post.time|date("d/m/Y")}}</small>

                    </span>
                    {{post.text}}
                </p>

            </div>


        {% endfor %}
    </div>

{% endblock %}
